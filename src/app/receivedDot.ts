export class ReceivedDot {
  currentTime: number;
  executionTime: number;
  id: number;
  pointInArea: boolean;
  r: number;
  // tslint:disable-next-line:variable-name
  user_id: number;
  x: number;
  y: number;
}
